name: Streak

on:
    schedule:
        - cron: "0 0 * * 1"

jobs:
    build:
        runs-on: ubuntu-latest
        steps:
        # check out repo to github_workspace
        - uses: actions/checkout@v2

        #fetch latest streak
        - name: Fetch Streak
        run: ./streaktaker
            